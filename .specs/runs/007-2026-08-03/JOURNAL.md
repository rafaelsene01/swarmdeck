# Run 007 (spec-loop) — 03/08/2026

**Status:** concluída
**Modo:** direto — escolhido pelo usuário antes da primeira onda
**Triagem que autorizou:** TRIAGE.md desta pasta (007), revisão `259b96e` (bate com o HEAD atual, sem mudança de código desde o fechamento). Inventário executável herdado de `.specs/runs/006-2026-08-02/TRIAGE.md` (007 verificou e não alterou o inventário).
**Orquestrador:** sessão iniciada em 03/08/2026

## Fila de execução (da triagem)

| Item | Feature | Escopo/gate | Classificação | Onda | (se sozinha) por quê |
|---|---|---|---|---|---|
| T13 — persistência do último diretório do seletor | multi-terminal | rust full | code | 1 | — |
| T7 — montar `/kanban` real + payload `task_changed` | task-kanban | build | code | 1 | — |
| T3 — configurar git-cliff | release-distribution | build | code | 1 | — |
| T14 — plugin de diálogo nativo + comandos | multi-terminal | build | code | 2 | toca `lib.rs` (território compartilhado) |
| T20 — remedir bytes do binário com/sem perfil | release-distribution | build | code | 2 | — |
| T8 — formulário de criação manual | task-kanban | quick | code | 2 | — |
| T15 — seletor de pasta no `NewTerminalDialog` | multi-terminal | quick | code | 3 | — |
| T16 — rename manual do terminal | multi-terminal | build | code | 3 | toca `lib.rs` + `commands/terminal.rs` (território compartilhado) |
| EXECUTION.md — recontar tasks | (transversal) | — | code (bookkeeping) | 3 | — |
| T5 — expor catálogo de agentes + honrar escolha no spawn | agent-selection | build | code | 4 | toca `lib.rs` + `App.tsx` (território compartilhado) |
| T5 — integrar badge/log ao `TerminalHeader` real | terminal-statuses | quick | code | 4 | toca `TerminalHeader.tsx`, colide com T16 (onda 3) |
| T1 — janela secundária de Configurações | settings-shell | build | code | 5 | toca `lib.rs` + `App.tsx`, colide com `agent-selection/T5` (onda 4) |
| T2 — navegação + montagem dos 4 painéis | settings-shell | build | code | 6 | depende de T1 |

**Justificativa das ondas (colisão de arquivo — território compartilhado do Perfil):** `lib.rs` (registro central de comandos) é tocado por `multi-terminal/T14`, `multi-terminal/T16`, `agent-selection/T5` e `settings-shell/T1` — as quatro serializadas entre si. `App.tsx` é tocado por `agent-selection/T5` e `settings-shell/T1` — colidem. `TerminalHeader.tsx` é tocado por `multi-terminal/T16` e `terminal-statuses/T5` — colidem. `commands/terminal.rs` é tocado por `multi-terminal/T14` e `multi-terminal/T16` — colidem.

## Execução

| Task | Onda | Implementador | Gates | Validação | Ciclos | Status |
|---|---|---|---|---|---|---|
| release-distribution/T3 | 1 | Completo (sem edição necessária — `cliff.toml` já correto) | `cargo build` OK (46.35s, 1 warning inofensivo do linker); `npm run build` FALHOU por erro TS em `KanbanBoard.tsx`, arquivo em edição concorrente por `task-kanban/T7` na mesma onda, não território de T3; `git cliff --unreleased` via `npx git-cliff 2.13.1` rodou de verdade (não simulado) — 9 tipos + `style` mapeados corretamente, commit não convencional cai em "Outros", `chore(release):` é `skip`ado | — (modo direto) | 0 | Completo |
| task-kanban/T7 | 1 | Completo | `cargo build` limpo; `npm run build` limpo (tsc+vite); `cargo test commands::tasks` 6/6 (baseline 6); `npm run test -- useTaskStore` 7/7; `cargo test tasks::send` 7/7; extra: `cargo test --test ipc_server` 15/15, `npm run test` completo 67/67 (15 arquivos), `cargo test` workspace 100+/0 falhas, `cargo clippy --all-targets -- -D warnings` limpo. `cargo fmt --all -- --check` reportou 1 diff só em `picker_prefs.rs` (arquivo de T13, em edição concorrente, não território de T7). DESVIO: (1) `route_create_project` deixou de emitir `task_changed` — projeto não é tarefa e o payload agora é tipado; (2) tocou `commands/tasks.rs` e `tests/ipc_server.rs` (fora do "Onde" original) para acompanhar a nova assinatura de `emit_task_changed`, ambos fora da lista de território proibido; (3) `get_with_project` virou `pub(crate)` para reuso; (4) `IpcServer::for_app` segue não fiado em `run()/setup()` — gap pré-existente, fora do escopo de T7 (lib.rs é território proibido). Não verificado: Verify de UI real via uat-agent (janela Kanban real, card migrando ao vivo, enviar-ao-terminal) — pendente, modo direto não roda UAT. | — (modo direto) | 0 | Completo |
| task-kanban/T8 | 2 | Bloqueado — investigou, não editou nada | não aplicável (nenhum gate rodado — nenhuma mudança de código) | — (modo direto) | 0 | Devolvido à triagem (⛔ NEEDS-DECISION) |
| multi-terminal/T14 | 2 | Completo | `cargo build` limpo; `cargo clippy --all-targets -- -D warnings` limpo; `cargo test` (raiz do workspace) 184/0 falhas/16 suítes — igual ao baseline da onda 1; `npm run build` limpo. Runtime: app subiu de verdade via `npm run tauri dev`, sem panic/erro de ACL no boot (capability `dialog:default` confirmada por leitura do `permissions/default.toml` real do crate, não assumida). Arquivos confirmados pelo orquestrador via grep: `terminal_picker_last_dir`/`terminal_picker_set_last_dir` em `commands/terminal.rs` e registrados em `lib.rs`; `tauri-plugin-dialog` em `Cargo.toml`; `dialog:default` em `capabilities/default.json`; `@tauri-apps/plugin-dialog` em `package.json`. Não verificado: round-trip dos comandos pelo console do devtools (sem canal de automação de UI/CDP disponível no ambiente) — coberto indiretamente pelos 4 testes de `picker_prefs` (T13). | — (modo direto) | 0 | Completo |
| release-distribution/T20 (remedição) | 2 | Completo — só medição, sem edição de código (retomado 1x — 1ª resposta parou em "builds em andamento", 2ª trouxe os números) | `cargo build --release -p swarmdeck`, 2 builds em `CARGO_TARGET_DIR` separados: **com perfil** 13.596.672 bytes (≈12,97 MiB); **sem perfil** (`git worktree` descartável, removido ao final) 13.970.944 bytes (≈13,32 MiB). Redução 374.272 bytes / ~2,68% (bem menor que os ~20,8% de 31/07 — binário cresceu desde então). Meta <20MB atingida nos dois casos. `STATE.md` atualizado pelo orquestrador (não pelo subagent) com a remedição, preservando a medição histórica de 31/07. Achado relatado com precisão: durante a medição, `git status --short` mudou (`Cargo.toml`, `lib.rs`, `capabilities/default.json`, `commands/terminal.rs`, `package.json`, `Cargo.lock`, `package-lock.json`) — confirmado que é o implementador paralelo de `multi-terminal/T14` (mesma lista de arquivos do seu "Onde"), não o subagent de medição. Nenhum worktree/diretório temporário sobrou. | — (modo direto) | 0 | Completo |
| settings-shell/T2 | 6 | Completo | `npm run build` limpo; `cargo build` limpo (nenhum arquivo Rust tocado); `npm run test` 72/72, 15 suítes (sem regressão). Arquivos confirmados via grep: `renderForLabel` com ramo `'settings'` em `main.tsx`; `AgentPanel`/`ProjectsPanel`/`StatusesPanel`/`UpdateSettings` montados em `SettingsShell.tsx`. Branch `kanban` confirmado intacto. DESVIO/achado real, documentado nas specs pelo próprio implementador: boa parte dos painéis roda com dado real (`agent_catalog`, `project_list`, `update_check`, já expostos), mas várias ações de escrita (`set_default_agent`, todo o CRUD de `status_catalog`, `set_auto_check`) têm lógica de backend pronta e testada mas SEM invólucro `#[tauri::command]` — ficou fora do escopo (exigiria tocar `commands/agents.rs`, um `commands/statuses.rs` novo, `commands/update.rs` e `lib.rs`, todos território fechado desta task). Registrado em `settings-shell/tasks.md`/`spec.md` como candidato a um `SET-03` futuro. **Achado adicional, confirmado pelo orquestrador**: `src-tauri/capabilities/default.json` só lista `"windows": ["main"]` — nem a janela `kanban` nem `settings` aparecem na capability, o que sob o ACL do Tauri 2 sugere que nenhum `invoke()` dessas janelas teria permissão em runtime real. É risco pré-existente e transversal (já valeria também para `task-kanban/T7`), não introduzido por esta task — só uma UAT real confirma se é bloqueante. Não verificado: Verify de UI real via uat-agent (modo direto). | — (modo direto) | 0 | Completo, com achado transversal de ACL para reportar à próxima triagem |
| settings-shell/T1 | 5 | Completo | `cargo build` limpo; `npm run build` limpo; `cargo test` 184/0 falhas/16 suítes (sem regressão); `npm run test` 72/0 falhas/15 arquivos (sem regressão). Arquivos confirmados via grep: `settings_open`/`settings_focus_main` em `windows/settings.rs`, registrados em `lib.rs`; botão "Configurações" em `App.tsx` chamando `invoke('settings_open')`. DESVIO: envolveu os 2 botões da toolbar num `<div>` flex para preservar `space-between` — decisão de layout mínima, sem afetar requisito. Confirmou que a edição de `App.tsx` da onda 4 (catálogo de agentes) continua intacta. Implementador também atualizou `spec.md`/`tasks.md` de settings-shell (checkboxes + rastreabilidade SET-01 "Done no gate", Verify anotado como pendente) — aceito, é bookkeeping de execução análogo ao já feito por outros implementadores desta run com marcadores SPEC. Não verificado: Verify de UI real (modo direto). | — (modo direto) | 0 | Completo |
| agent-selection/T5 | 4 | Completo | `cargo build` limpo; `npm run build` limpo; `cargo test` 184/0 falhas/16 suítes (sem regressão); `npm run test` 72/0 falhas/15 arquivos (sem regressão); `cargo clippy` limpo; `cargo fmt --check` isolado em `agents.rs` limpo (2 diffs preexistentes em arquivos de outra task, não tocados). Arquivos confirmados via grep: `agent_catalog`/`agent_default` em `commands/agents.rs` e registrados em `lib.rs`; `agentByTerminalId`/`invoke('agent_catalog')`/`invoke('agent_default')` em `App.tsx`. DESVIO: tocou `commands/mod.rs` (`pub mod agents;`) fora da lista fechada, mecanicamente necessário para o módulo compilar. Não verificado: UAT real com CLI de agente instalado (modo direto). | — (modo direto) | 0 | Completo |
| terminal-statuses/T5 | 4 | Completo | `npm run build` limpo (após corrigir 1 erro de TS `noUncheckedIndexedAccess`); `cargo build` limpo; `npm run test` 72/0 falhas/15 arquivos (sem regressão). Arquivos confirmados via grep: `StatusBadge`/`ActivityLog` importados e renderizados em `TerminalHeader.tsx`; rename manual de T16 (`commitEditingTitle` etc.) confirmado intacto pelo próprio implementador e reconfirmado pelo orquestrador via grep. DESVIO: prop `activity` (singular) virou `activities` (array) para casar com a interface de `ActivityLog` — nenhum outro arquivo consumia a prop antiga, troca segura. `App.tsx` ainda não passa `status`/`statusColor`/`activities` reais — mesmo gap de integração já visto em T16 (componente pronto, produtor do dado ainda não fiado). Não verificado: UAT real (modo direto). | — (modo direto) | 0 | Completo, com gap de integração documentado (App.tsx não alimenta os dados ainda) |
| multi-terminal/T16 | 3 | Completo (dentro do escopo autorizado) | `cargo build --workspace` limpo; `npm run build` limpo; `cargo test --workspace` 184/0 falhas/16 suítes (sem regressão); `npm run test` 72/0 falhas/15 arquivos (sem regressão); `cargo clippy` limpo; `cargo fmt --check` reportou 2 diffs, nenhum no código desta task (em `terminal_picker_set_last_dir`/`picker_prefs.rs`, de outros implementadores). Arquivos confirmados pelo orquestrador via grep: `terminal_set_title` em `commands/terminal.rs` e `lib.rs`; `commitEditingTitle`/`cancelEditingTitle`/`TerminalMetaService` em `TerminalHeader.tsx`; `app.manage(...TerminalMetaService::new())` em `lib.rs`. DESVIO: (1) adicionou `app.manage(TerminalMetaService::new())` em `lib.rs`, fora da instrução literal mas mecanicamente necessário (sem estado gerido o comando falharia em runtime); (2) `id` da prop de `TerminalHeader` ficou opcional porque `App.tsx` hoje não passa nenhum id. **ACHADO IMPORTANTE, fora do escopo desta task**: o id real da sessão (`TerminalId` de `pty_spawn`) só existe hoje numa variável local dentro de `TerminalPane.tsx` — nunca sobe a `App.tsx` nem desce a `TerminalHeader`. Ou seja, mesmo com a UI de rename pronta, **não há caminho real no app hoje para ela funcionar ponta a ponta** — precisa de uma task futura conectando `TerminalPane` → `App.tsx` → `TerminalHeader` (mesma classe de gap que originou `T12`). Não verificado: Verify de UI real via uat-agent (modo direto não roda UAT; e mesmo se rodasse, o gap acima o impediria hoje). | — (modo direto) | 0 | Completo, com gap de integração documentado (ver Não verificado) |
| multi-terminal/T15 | 3 | Completo | `npm run test -- NewTerminalDialog` 6/6 (1 pré-existente ajustado + 5 novos); `npm run test` completo 72/72 (15 arquivos); `npm run build` limpo; `cargo build --workspace` limpo. Arquivos confirmados pelo orquestrador via grep: `readOnly` no input, `terminal_picker_last_dir`/`terminal_picker_set_last_dir` via `invoke()`, `plugin-dialog` importado. DESVIO: teste pré-existente reescrito porque "criar" agora fica `disabled` sem `cwd` (TERM-10 AC5, consequência direta do critério pedido, não escolha livre). Não verificado: diálogo nativo real do SO (fora de CDP) — verificação manual do mantenedor; `cargo clippy`/`fmt` não rodados (task é só frontend). | — (modo direto) | 0 | Completo |
| EXECUTION.md (bookkeeping) | 3 | Escrito diretamente pelo orquestrador (documentação de nível projeto — não delegado) | `grep -cE '^### T'` nos 7 tasks.md de M1/M2: 20+6+10+9+5+8+2 = 60 (era 38); `grep -nE '[P]'` nos headers: 17 (era 13, só `mt/T17-T20` se somaram à lista antiga); `grep -c "MCP: NENHUM"` bate 60/60 com o total de headers. Mermaid + tabela onda-a-onda NÃO foram re-derivados para as 22 tasks novas (registrado explicitamente no próprio arquivo como não feito, para não inventar dependência não verificada) | — (modo direto) | 0 | Completo |
| multi-terminal/T13 | 1 | Completo (retomado 1x — 1ª resposta veio incompleta, "vou aguardar notificação", sem relatório; SendMessage pediu o relatório de novo e a 2ª resposta veio completa) | `cargo test` (workspace, isolado num git worktree temporário para não ler o `ipc/server.rs` em edição concorrente de T7) — **184 passando / 0 falhas / 16 suítes**, incluindo os 4 novos de `picker_prefs::tests`. Arquivos confirmados no disco pelo orquestrador (`ls`): `db/migrations/005_terminal_picker_prefs.sql`, `terminal/picker_prefs.rs`. DESVIO: (1) registrou a migração `005` em `src-tauri/src/db/mod.rs` (array `MIGRATIONS`) — necessário estruturalmente, sem isso a migração nunca roda; arquivo não está na lista de território compartilhado do perfil; (2) atualizou 3 testes de integração pré-existentes (`tests/db.rs`, `tests/settings.rs`, `tests/tasks_schema.rs`) que tinham `schema_version() == 4` hardcoded — trocado para `5`, só o literal, nenhuma outra asserção alterada. Não verificado: `cargo fmt`/`cargo clippy` não rodados nos arquivos novos (só o gate `cargo test` era exigido); `.db` não inspecionado visualmente fora dos testes; compilação conjunta com o resultado final de `ipc/server.rs` (T7) não testada por T13 (mas T7 já reporta gate verde depois, e os arquivos são disjuntos). | — (modo direto) | 0 | Completo |

(preenchida conforme cada task termina)

## Devolvido para triagem

| Task | Pergunta | Onde ficou gravada | Estado do código |
|---|---|---|---|
| task-kanban/T8 | O único caminho de criação de tarefa (`tasks::service::create`) exige `TerminalContext`/deriva `project_id` do `cwd` — não aceita projeto explícito. 3 opções: (1) expandir T8 para incluir `task_create` no backend + decidir semântica de `terminal_id` manual; (2) quebrar em task de backend + task de UI; (3) reduzir o critério 1 desta rodada (sem seletor de projeto) com AD explícita admitindo a redução | `.specs/features/task-kanban/tasks.md`, header de T8 (marcador `⛔ NEEDS-DECISION`) | Nada alterado — nenhum arquivo tocado pela tentativa |

## Gates medidos nesta run

| escopo | comando | resultado medido | quando |
|---|---|---|---|
| rust — testes (isolado, worktree temporário) | `cargo test` | 184 passando / 0 falhas / 16 suítes | Onda 1, T13 |
| rust — testes (repo real, pós T7) | `cargo test commands::tasks` / `tasks::send` / `--test ipc_server` | 6/6, 7/7, 15/15 | Onda 1, T7 |
| front — testes | `npm run test` | 67 testes / 15 arquivos / 0 falhas | Onda 1, T7 |
| release — git-cliff real | `npx git-cliff --config cliff.toml --unreleased` (+ `--with-commit`) | exit 0, 9 tipos + `style` mapeados corretamente, não-convencional → "Outros" | Onda 1, T3 |
| release — bytes do binário | `cargo build --release -p swarmdeck` (com e sem perfil, `CARGO_TARGET_DIR` separados) | com perfil 13.596.672 bytes; sem perfil 13.970.944 bytes; redução ~2,68% (era ~20,8% em 31/07 — binário cresceu); meta <20MB atingida nos dois casos | Onda 2, T20 |
| rust — testes (final, pós onda 6) | `cargo test` (raiz do workspace) | 184 passando / 0 falhas / 16 suítes, estável desde a onda 1 até o fim da run | Ondas 2-6, reconfirmado por T14/T15/T16/T5(agt)/T5(stat)/T1/T2 |
| front — testes (final, pós onda 6) | `npm run test` | 72 testes / 15 arquivos / 0 falhas (subiu de 67 para 72 com os 5 testes novos de T15) | Ondas 3-6 |
| rust — lint (final) | `cargo clippy --all-targets -- -D warnings` | limpo, 0 warnings, reconfirmado por T14/T16/T5(agt) | Ondas 2, 3, 4 |

## Estado da árvore no último checkpoint

`git rev-parse --short HEAD` → `259b96e` (nenhum commit feito pela skill; run trabalha só na working tree)

**Após onda 1** (`git status --short`): além dos 16 arquivos de `.specs`/`AGENTS.md` já modificados antes desta run, ficaram modificados: `src-tauri/src/{commands/tasks.rs, db/mod.rs, ipc/server.rs, terminal/mod.rs}`, `src-tauri/tests/{db.rs, ipc_server.rs, settings.rs, tasks_schema.rs}`, `src/main.tsx`, `src/routes/kanban/KanbanBoard.tsx`; novos: `src-tauri/src/db/migrations/005_terminal_picker_prefs.sql`, `src-tauri/src/terminal/picker_prefs.rs`.

**Checkpoint final (fim da run, `git status --short`)**: modificados (além dos 16 de `.specs`/`AGENTS.md` pré-existentes à run): `.specs/project/EXECUTION.md`, `Cargo.lock`, `package-lock.json`, `package.json`, `src-tauri/Cargo.toml`, `src-tauri/capabilities/default.json`, `src-tauri/src/commands/{mod.rs, tasks.rs, terminal.rs}`, `src-tauri/src/db/mod.rs`, `src-tauri/src/ipc/server.rs`, `src-tauri/src/lib.rs`, `src-tauri/src/terminal/mod.rs`, `src-tauri/tests/{db.rs, ipc_server.rs, settings.rs, tasks_schema.rs}`, `src/App.tsx`, `src/components/terminal/{NewTerminalDialog.tsx, NewTerminalDialog.test.tsx, TerminalHeader.tsx}`, `src/main.tsx`, `src/routes/kanban/KanbanBoard.tsx`. Novos: `src-tauri/src/commands/agents.rs`, `src-tauri/src/db/migrations/005_terminal_picker_prefs.sql`, `src-tauri/src/terminal/picker_prefs.rs`, `src-tauri/src/windows/settings.rs`, `src/routes/settings/SettingsShell.tsx`. Nenhum commit feito pela skill em nenhum momento — HEAD segue `259b96e`.

## Não verificado

- Verify de UI real via `uat-agent` em `task-kanban/T7` e `multi-terminal/T15`/`T16`/`T14` — modo direto não roda UAT nesta run.
- **Gap de integração descoberto em `multi-terminal/T16`, fora do escopo daquela task**: o `TerminalId` real (devolvido por `pty_spawn`) só existe hoje numa variável local de `TerminalPane.tsx` — nunca chega a `App.tsx` nem a `TerminalHeader.tsx`. A UI de rename manual (T16) e o comando `terminal_set_title` estão prontos e compilam, mas **não há caminho real no app hoje para exercitá-los ponta a ponta** — precisa de uma task nova conectando `TerminalPane` → `App.tsx` → `TerminalHeader` (mesma classe de gap que originou `multi-terminal/T12`). Recomendado reportar à próxima `spec-triage`.
- Round-trip real (via console do devtools) dos comandos `terminal_picker_last_dir`/`terminal_picker_set_last_dir` (T14) — sem canal de automação de UI/CDP disponível no ambiente do implementador; coberto indiretamente pelos testes de `picker_prefs`.
- **Achado transversal de ACL, descoberto em `settings-shell/T2`, fora do escopo daquela task**: `src-tauri/capabilities/default.json` só lista `"windows": ["main"]` — nem a janela `kanban` (já existente antes desta run) nem `settings` (nova, T1 desta run) aparecem na capability. Sob o ACL do Tauri 2, isso sugere que nenhum `invoke()` dessas duas janelas teria permissão em runtime real — o que afetaria `task-kanban/T7` (que já chama `invoke('task_delete')`/`invoke('task_send')` da janela `kanban`) e toda a navegação de `settings-shell/T2`. Não corrigido nesta run (exigiria editar `capabilities/default.json`, e nenhuma task da fila autorizava isso para este fim). Recomendado reportar à próxima `spec-triage` com prioridade — se confirmado, é um bug real que bloquearia o uso de duas janelas inteiras.
- **Gap encontrado em `settings-shell/T2`**: os 4 painéis de Configurações leem dado real (`agent_catalog`, `project_list`, `update_check`), mas várias ações de escrita têm lógica de backend pronta e testada sem invólucro `#[tauri::command]` (`agents::prefs::set_default_agent`, todo o CRUD de `terminal::status_catalog`, `db::set_auto_check`). Candidato a uma task nova (`SET-03` ou equivalente) — já anotado em `settings-shell/tasks.md`/`spec.md` pelo implementador.
- `task-kanban/T8` não foi implementada — estacionada por decisão de arquitetura pendente (ver "Devolvido para triagem").
