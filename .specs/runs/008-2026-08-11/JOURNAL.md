# Run 008 — 11/08/2026 (spec-loop)

**Status:** pausada (onda 3 despachada e depois revertida em 12/08/2026 — 2 dos 3 implementadores foram cortados pelo limite de sessão da API no meio do trabalho, ver "Incidente" abaixo; retomar após 18h30 America/Sao_Paulo)
**Triagem que autorizou:** TRIAGE.md desta pasta, revisão `6f93e6b`
**Orquestrador:** sessão iniciada às ~19:40 (11/08); retomada 11/08 (~21h, resolução do ponto de pausa); retomada de novo em 12/08/2026 (onda 2); retomada de novo em 12/08/2026 (onda 3, após reconciliação de 2 decisões do usuário)

**Retomada de 12/08/2026 (2ª) — Fase 0/4 (frescor):** `git status --short` no início desta sessão mostrou 2 arquivos modificados além do checkpoint da onda 2 (`.specs/features/mcp-task-server/tasks.md`, `.specs/project/STATE.md`) — divergência investigada: `git diff` confirma que são as 2 decisões do usuário devolvidas à triagem ao fim da onda 2 (`projects/T5` — opção (b), migrar `create` de verdade; `terminal-statuses/T5` fundida com `mcp-task-server/T9`, decisão de arquitetura Opção B), resolvidas numa sessão de `tlc-spec-driven` fora desta skill e já gravadas em `projects/tasks.md::T5` e `mcp-task-server/tasks.md::T9`. Nenhuma divergência inexplicada — `TRIAGE.md` continua valendo. `cargo fmt --all -- --check` conferido limpo (exit 0, sem regressão desde a onda 1).

**Onda 3 despachada (12/08/2026):** com as 2 decisões resolvidas, 3 itens da fila original ficam com arquivos disjuntos e prontos: `projects/T5` (`service.rs`, `commands/projects.rs`, `tests/projects.rs`), `mcp-task-server/T9` fundida (`lib.rs`, `ipc/server.rs`, `App.tsx`), `agent-selection/T6` (`agents/session.rs` novo, `agents/launch.rs`, migração `006`, `NewTerminalDialog.tsx`). Implementadores despachados em paralelo (3, dentro do teto). Validação pendente — ver Execução ao concluírem.

**Erro de planejamento achado pelo implementador de `projects/T5` (registrado para não repetir):** a lista fechada de arquivos que passei ("2 chamadores da `create` antiga") estava incompleta — `grep` real do implementador achou 3 chamadores a mais: `src-tauri/src/ipc/server.rs:727` (handler MCP `route_create_project`, produção), `src-tauri/tests/task_service.rs` (5 call sites) e `src-tauri/src/commands/tasks.rs` (3 call sites em `#[cfg(test)]`). O `tasks.md::T5` original também não citava esses arquivos — o gap já estava na spec, eu só herdei sem conferir com `grep` antes de fechar a lista. Mudar a assinatura de `create` sem tocar os 9 call sites quebra o build. `ipc/server.rs` está sendo editado agora mesmo pelo implementador de `mcp-task-server/T9` (onda 3, em paralelo) — colisão real se eu ampliar o escopo de T5 imediatamente. Implementador corretamente devolveu Status: Bloqueado em vez de adivinhar ou invadir o arquivo do outro. **Não é NEEDS-DECISION de produto** (a decisão "API final = uma função só" já foi tomada em 12/08/2026 e não admite segunda leitura válida — a opção B que o implementador listou reabriria essa AD já fechada, então não é uma opção real) — é erro de escopo meu, resolvido como orquestrador: `projects/T5` fica sequenciada para depois que `mcp-task-server/T9` terminar de editar `ipc/server.rs`, com o escopo de arquivos ampliado para os 9 call sites reais. Re-despacho após a notificação de conclusão de `T9`.

**Incidente: onda 3 interrompida pelo limite de sessão da API, `mcp-task-server/T9` e `agent-selection/T6` revertidas (12/08/2026).** Os dois implementadores em paralelo foram cortados no meio do trabalho — nenhum devolveu relatório final nem número de gate medido; a última mensagem de cada um narrava o PRÓXIMO passo pendente ("Now let's update `lib.rs`...", "Now let's re-check the Rust side once more, and run the full frontend test suite too"), confirmando que nenhum tinha terminado. Investigação antes de decidir:

1. `git status --short` mostrou os dois terem escrito no disco de verdade (não foi um bloqueio limpo): `agents/launch.rs`, `agents/mod.rs`, `db/mod.rs`, `ipc/server.rs`, `lib.rs`, `terminal/picker_prefs.rs`, 3 arquivos de `tests/*.rs`, `NewTerminalDialog.tsx`+`.test.tsx` modificados; `agents/session.rs`, `db/migrations/006_agent_sessions.sql` novos.
2. `cargo build --workspace` **passava** (só o warning de linker pré-existente) — a árvore compilava.
3. `cargo test --workspace` **travava** com `STATUS_ENTRYPOINT_NOT_FOUND` (0xC0000139) ao carregar `swarmdeck_lib-<hash>.exe` — um erro de carregador do Windows (DLL sem o ponto de entrada esperado), não uma falha de teste Rust normal.
4. Descartei corrupção de cache/artefato incremental: `cargo clean -p swarmdeck` (35.2 GiB removidos) → mesmo erro; `cargo clean` total (16.2 GiB removidos, rebuild do zero de TODAS as dependências, 2m07s) → **mesmo erro, idêntico**. Confirmado via PowerShell (`Start-Process -Wait`, não só o wrapper bash) que o código de saída real é `-1073741511` = `0xC0000139` — não é artefato de como o bash interpreta o processo.
5. Li o diff completo de `ipc/server.rs` e `lib.rs`: a mudança (redesenho `Backend::Owned`/`Backend::App`, Opção B) é tecnicamente sólida e bem comentada — nenhum problema óbvio de Rust (o `cargo build` provava isso). A causa raiz do `ENTRYPOINT_NOT_FOUND` ficou **não identificada** — é um problema de link/carregador do Windows abaixo do nível que dá para depurar com as ferramentas disponíveis aqui (sem `dumpbin` no PATH), possivelmente ligado ao crate ser `["staticlib", "cdylib", "rlib"]` e o binário de teste, por algum motivo, depender de carregar o `.dll` separadamente — mas isso não muda a decisão: os dois implementadores não tinham terminado, e eu não tenho como despachar mais agentes para investigar (limite de sessão até 18h30 America/Sao_Paulo).

**Decisão (orquestrador, sem novo agente):** revertidos ao `HEAD` os 11 arquivos modificados por `T9`/`T6` e apagados os 2 arquivos novos (`session.rs`, migração `006`) — nada disso estava commitado, então não há histórico sendo reescrito, só trabalho não finalizado e não verificável descartado. Preservado intocado: `projects/mod.rs`+`sandbox.rs` (onda 2, já aprovados) e todo `.specs/**`. Também apaguei 2 arquivos de diagnóstico temporário que eu mesmo criei durante a investigação (`_test_stdout.txt`, `_test_stderr.txt`, fora do padrão do projeto). **Confirmado limpo depois da reversão:** `cargo test --workspace` → **192 passando / 0 falhas** (soma de cada `test result:` da corrida — mesmo baseline da onda 2, nenhuma regressão), `cargo fmt --all -- --check` → exit 0.

**Run pausada aqui.** `projects/T5` (bloqueada por erro de escopo meu, ver acima) e `mcp-task-server/T9`+`agent-selection/T6` (revertidas, para redespachar do zero) ficam para a próxima retomada, depois do reset do limite de sessão (18h30 America/Sao_Paulo, 12/08/2026). Nenhuma decisão nova do usuário é necessária para retomar — é reexecução, não re-triagem.

**Ponto de pausa anterior (resolvido na retomada de 11/08):** as 2 alegações pendentes de validação foram revalidadas por agentes novos, em paralelo. `multi-terminal/T16` → **APROVADO** (ver Execução). `projects/T5` → **NEEDS-DECISION** (ver Execução e Devolvido para triagem) — o código funciona e está testado, mas diverge do `Done when` literal (`create_with_options` nova em vez de `create` migrada); estacionada, não marcada como Done. Onda 2+ ficou sem despacho até esta sessão.

**Retomada de 12/08/2026 — Fase 0/4 (frescor):** `git rev-parse --short HEAD` → `e8b02a8`, avançou de `6f93e6b` (revisão de fechamento da triagem). Divergência investigada antes de prosseguir: `git log --oneline 6f93e6b..e8b02a8` mostra 2 commits — `99354f2` (`git diff --stat` bate exatamente com o trabalho já documentado nesta run: `projects/service.rs`, `App.tsx`, `TerminalPane.tsx`, `commands/terminal.rs`, `terminal/picker_prefs.rs` — é o mantenedor commitando à mão o que a retomada de 11/08 já tinha implementado e validado) e `e8b02a8` (`graphify-out/**` apenas — saída do skill `graphify`, nenhum arquivo de código ou `.specs/**`). Nenhuma divergência inexplicada — `TRIAGE.md` continua valendo, retomada segue sem pedir nova triagem. `git status --short` → árvore limpa no início desta sessão.

**Achado desta sessão, antes de despachar a onda 2:** `terminal-statuses/T5` (primeira da fila, onda 2) tem uma dependência oculta que a triagem não capturou — o escopo novo (evento push) exige `IpcServer` rodando contra um `AppHandle` real, e isso nunca foi ligado em `lib.rs` (é o gap que `mcp-task-server/T9` existe para fechar, e `T9` está parada com `NEEDS-DECISION` própria desde a run 005). Estacionada sem despachar implementador — ver `Devolvido para triagem`. Onda 2 redefinida com os 2 itens da fila original que não dependem disto: `projects/T9` (Sandbox) e `release-distribution/T3` (git-cliff local), mais o item de bookkeeping (`release-distribution` — reconciliar contagens de `test:scripts`), já que os três têm arquivos disjuntos.

**Erro de planejamento nesta sessão, achado pelo validador de `T3` (registrado para não repetir):** classifiquei `release-distribution/T3` e a task de bookkeeping ("reconciliar contagens de `test:scripts`") como "arquivos disjuntos" — mas os dois escrevem no MESMO arquivo, `.specs/features/release-distribution/tasks.md`, só que em seções diferentes (T3 em ~118-122; a reconciliação em T1/T7/T8, linhas 52/220/247). A regra da skill (Fase 1) é explícita: a checagem é por ARQUIVO, não por faixa de linha — eu apliquei errado. Consequência real: nenhuma corrupção (as edições não se sobrepuseram, o `git diff` final está limpo e correto), mas o validador de `T3`, ao rodar `git diff` sobre o arquivo inteiro, viu a edição da linha 247 (que pertence à task de reconciliação, feita em paralelo) e atribuiu erroneamente ao implementador de `T3`, reprovando por "alegação de escopo falsa". Confirmado por leitura direta do diff completo (`git diff -- .specs/features/release-distribution/tasks.md`) que as duas edições são de tasks diferentes, não se tocam, e cada implementador relatou corretamente o que ELE tocou. Tratado como falso-positivo do validador causado por erro meu de planejamento, não como defeito do implementador de `T3` — ver veredito final em `Execução`.

## Fila de execução (da triagem)

| Item | Feature | Escopo/gate | Classificação | Onda | Nota |
|---|---|---|---|---|---|
| `cargo fmt --all` | (transversal) | rust build | `code` | 1 | formata `commands/terminal.rs`, `terminal/picker_prefs.rs` |
| `T16` — `id` real em `App.tsx`→`TerminalHeader` | multi-terminal | front build | `code` | 1 | confirmado por leitura direta: backend (`terminal_set_title`) e UI de duplo-clique JÁ EXISTEM; único gap real é `App.tsx` não passar `id={terminal.id}` |
| `T5` PROJ-01/02/09 (base-dir, cor, git init) | projects | rust full | `code` | 1 | `service.rs` |
| `T5` evento push de status/atividade | terminal-statuses | rust+front full | `code` | 2 | `ipc/server.rs` + `App.tsx` (depende de T16 ter liberado `App.tsx`) |
| `T6` last-opened + `project_list_recent` | projects | rust full | `code` | 2 | `service.rs` (depende de T5 projects ter liberado `service.rs`) + migração **006** |
| `T9` pseudo-projeto Sandbox | projects | rust full | `code` | 2 | `sandbox.rs`, novo, independente |
| `T6` Resume/New Session | agent-selection | rust+front full | `code` | 3 | `session.rs`, migração **007**, `launch.rs`, possivelmente `commands/terminal.rs`+`lib.rs`, `NewTerminalDialog.tsx`, `App.tsx` (depende de terminal-statuses/T5 ter liberado `App.tsx`) |
| `T7` `CreateProjectModal` | projects | front quick | `code` | 3 | novo arquivo, depende de projects/T5 (onda 1) |
| `T3` git-cliff local | release-distribution | build | `code` | 3 | `cliff.toml` + instalação local |
| `T8` `ProjectPicker` | projects | front quick | `code` | 4 | novo arquivo, depende de projects/T6 (onda 2) |
| Reconciliar 3 contagens de `test:scripts` | release-distribution | doc | `code`/bookkeeping | 4 | só texto em `tasks.md` |

**Excluídos da fila apesar de "sim" no `TRIAGE.md` — achado desta run, não da triagem:**

| Item | Por quê excluído |
|---|---|
| `task-kanban/T8` (formulário manual) | O próprio `tasks.md` ainda tem o bloco `⛔ NEEDS-DECISION` da run 007, sem resposta registrada (pergunta de arquitetura: `task_create` novo vs. dividir em 2 tasks vs. reduzir critério). O `TRIAGE.md` 008 classificou como `code`/"sim" sem perceber a contradição — devolvido à triagem. |
| `mcp-task-server/T9` (iniciar `IpcServer` real) | Mesma situação: `tasks.md` linha 21 e o corpo da task (305-343) mantêm `⛔ NEEDS-DECISION — estacionada na run 005`, pergunta de arquitetura sobre `Arc`-wrapping de `TerminalManager`/`Db` nunca respondida. `TRIAGE.md` 008 classificou como "sim" sem notar. Devolvido à triagem. |

Ambos ficam marcados no `tasks.md` de origem (já estavam) — nenhuma edição nova feita agora, só o registro aqui e no relatório final de que a run 008 do `TRIAGE.md` errou a classificação desses dois itens.

## Execução

| Task | Onda | Implementador | Gates | Validação | Ciclos | Status |
|---|---|---|---|---|---|---|
| `projects/T5` (base-dir, cor, git init) | 1 | (sessão anterior, pós-pausa) | `cargo test --workspace` 189/189, `cargo test --lib projects::service::` 5/5, `cargo test --test projects` 8/8 intocado, clippy/fmt limpos | Validador novo (sessão de retomada): código correto e testado, mas achou desvio real — `Done when` fala em `create`, código entrega `create_with_options` nova, `create` original intocada | 1 | ⛔ **NEEDS-DECISION** — estacionada em `projects/tasks.md::T5`, `Done when` não marcado |
| `multi-terminal/T16` (`id` real de `App.tsx`→`TerminalHeader`) | 1 | (corretor, pós-pausa) | `cargo build` verde (1 warning de linker inofensivo), `npm run build` verde, `npm run test -- --run` 72/72 (15 arquivos), `cargo fmt --all -- --check` limpo | Validador novo (sessão de retomada): **APROVADO** — leu o código linha a linha (`TerminalHeader.tsx:103-109`, `TerminalPane.tsx:80`, `App.tsx:129-257`, `terminal.rs:52/113-119`), confirmou que a chave bate nos três pontos (front↔backend↔MCP) e rodou os gates ele mesmo, não confiou no relatório do corretor | 1 | ✅ **Aprovado** — checkboxes `[x]` do `Done when` (linha 570 de `tasks.md`) confirmados válidos |
| `projects/T9` (pseudo-projeto Sandbox) | 2 | implementador único | `cargo test --workspace` 192/192 (189 + 3 novos), fmt/clippy limpos | Validador novo: **APROVADO** — quebrou a implementação de propósito (removeu `fs::create_dir_all`), confirmou que 2 dos 3 testes falham de verdade, reverteu; confirmou reuso de `paths::data_dir` (autoridade única do projeto) em vez de reimplementar resolução de app-data-dir | 1 | ✅ **Aprovado** — `Done when` marcado em `projects/tasks.md::T9` |
| `release-distribution/T3` (git-cliff local) | 2 | implementador único | `cargo build && npm run build` verdes; `git cliff` instalado (`cargo install`, fora do repo) e rodado 2x com evidência real colada | Validador novo: **REPROVADO** por "alegação de escopo falsa" — viu, no `git diff` do arquivo inteiro, uma edição na linha 247 (T8) que na verdade pertence à task de reconciliação (rodando em paralelo no MESMO arquivo). Investiguei o `git diff` eu mesmo: os hunks de T3 (119/121/122) e os da reconciliação (52/220/247) não se sobrepõem e cada implementador relatou exatamente o que tocou — falso-positivo causado por eu ter despachado 2 tasks no mesmo arquivo em paralelo (ver nota acima, "Erro de planejamento"), não um defeito do implementador de T3 | 1 (override do orquestrador, sem 2º ciclo) | ✅ **Aprovado** — `Done when` marcado em `release-distribution/tasks.md::T3`, ressalva residual documentada (mapeamento `feat`/`fix` só por leitura estática, `git commit` de teste proibido neste repo) |
| `release-distribution` — reconciliar 3 contagens de `test:scripts` (T1/T7/T8) | 2 | implementador único → corretor (1 ciclo) | `npm run test:scripts` 27/27 medido 2x (implementador e corretor) | 1ª validação: **REPROVADO** (T7 linha 220 contradizia a linha vizinha; T1 linha 52 sem âncora temporal) → corretor resolveu ambos → 2ª validação: **REPROVADO** de novo por 2 motivos, ambos investigados e derrubados por mim: (a) "escopo falso" — mesmo falso-positivo de T3 acima, diff do arquivo compartilhado mostrando os hunks de T3 junto aos da reconciliação; (b) "aritmética de '17 pré-existentes' não fecha" — **conferi eu mesmo**: `bump-version`=11 + `make-portable`=6 (contagem *na época* da triagem 005, antes do fix 6→8 da triagem 006, linha 221) = 17; +8 de `patch-latest-json` (linha 248, "8 unit — confirmado", nunca mudou) = 25. Bate exatamente — o validador não tentou a soma 11+6 e concluiu por engano que não fechava | 1 corretor + override do orquestrador na 2ª validação (sem 3º ciclo — ambos os "defeitos" restantes eram falso-positivo verificável com evidência direta, não erro de conteúdo) | ✅ **Aprovado** — texto final em `tasks.md` (linhas 52, 220, 247) já correto, nenhuma edição adicional necessária |

**Nota sobre os overrides acima**: em ambos os casos onde reprovei a reprovação do validador, verifiquei pessoalmente com evidência direta (leitura do diff completo linha a linha, e a conta 11+6=17) antes de aceitar — não é "confiar no relatório do implementador por cima do validador", é uma terceira leitura independente que resolveu uma discordância entre dois agentes que eu, como orquestrador, tinha contexto suficiente (por ter despachado as tasks conflitantes) para arbitrar sem gastar mais um ciclo reescrevendo texto que já estava certo.

## Devolvido para triagem

| Task | Pergunta | Onde ficou gravada | Estado do código |
|---|---|---|---|
| `task-kanban/T8` | (já registrada na run 007, ver `tasks.md`) | `task-kanban/tasks.md::T8` | nada alterado nesta run |
| `mcp-task-server/T9` | (já registrada na run 005, ver `tasks.md`) | `mcp-task-server/tasks.md::T9` | nada alterado nesta run |
| `projects/T5` | `create_with_options` (função nova) cumpre o `Done when` que fala em `create` (função existente), ou a task exige migrar `create` de verdade? | `projects/tasks.md::T5` | `create_with_options` no disco, testada (5/5), `create` original intocada (8/8) — nada revertido, ambas as leituras aproveitam o código atual |
| `terminal-statuses/T5` | `IpcServer` não está ligado a um `AppHandle` real (`lib.rs` nunca chama `.serve()`), e ligar isso é o escopo represado de `mcp-task-server/T9` (parada desde a run 005, `NEEDS-DECISION` própria de arquitetura). Resolver `T9` primeiro, reduzir `T5` para só a fiação de `App.tsx` (emit fica `TODO`), ou fundir `T5`+`T9` numa task só? | `terminal-statuses/tasks.md::T5` | nada alterado — achado antes do despacho, nenhum implementador chegou a rodar |

## Gates medidos nesta run

| escopo | comando | resultado medido | quando |
|---|---|---|---|
| rust — testes (workspace) | `cargo test --workspace` | **189 passando, 0 falhas** (baseline 184 + 5 novos de `projects::service`) | validação de `projects/T5`, sessão de retomada 11/08 |
| rust — `projects::service` | `cargo test --lib projects::service::` | **5 passando** | validação de `projects/T5`, sessão de retomada 11/08 |
| rust — `tests/projects.rs` (T1, fora do escopo) | `cargo test --test projects` | **8 passando, intocado** | validação de `projects/T5`, sessão de retomada 11/08 |
| rust — lint | `cargo clippy --all-targets -- -D warnings` | exit 0 — 0 warnings | validação de `projects/T5`, sessão de retomada 11/08 |
| rust — formato | `cargo fmt --all -- --check` | exit 0 | validação de `projects/T5`, sessão de retomada 11/08 |
| rust — testes (workspace) | `cargo test --workspace` | **192 passando, 0 falhas** (189 + 3 novos de `projects::sandbox`) | validação de `projects/T9`, onda 2, 12/08 |
| rust — build | `cargo build` | sucesso (1 warning de linker pré-existente) | onda 2, 12/08 |
| front — build | `npm run build` | `✓ built in 2.19s`/`2.32s` (medido 2x, T3 e revalidação) | onda 2, 12/08 |
| scripts — `test:scripts` | `npm run test:scripts` | **27 passando, 0 falhas** (medido 3x independentes: implementador, validador de T9... não, da reconciliação, e revalidador) | onda 2, 12/08 |
| release — `git cliff` local | `git cliff --config cliff.toml --unreleased` / sem `--unreleased` | exit 0 nas 2 formas, sem `error`; commits de merge e o inicial não-convencional aparecem íntegros no catch-all `Outros` (esperado — 28/30 commits do histórico não usam prefixo Conventional Commits) | `release-distribution/T3`, onda 2, 12/08, reproduzido pelo validador |

## Estado da árvore no último checkpoint

**12/08/2026, fim da onda 2** — `git rev-parse --short HEAD` → `e8b02a8` (inalterado — nenhum commit feito por esta run, como sempre); `git status --short`:
```
 M .specs/features/projects/tasks.md
 M .specs/features/release-distribution/tasks.md
 M .specs/features/terminal-statuses/tasks.md
 M .specs/runs/008-2026-08-11/JOURNAL.md
 M src-tauri/src/projects/mod.rs
?? src-tauri/src/projects/sandbox.rs
```
Nada commitado — fica para o mantenedor revisar e commitar à mão, como o resto desta run.

## Não verificado

- **`multi-terminal/T16`, achado do validador (não bloqueia, não coberto pelo `Done when`)**: entre o mount do painel e `pty_spawn` resolver, `id` fica `undefined`. Se o usuário renomear nessa janela estreita, `TerminalHeader.tsx:102` atualiza o texto exibido mesmo assim, mas o `invoke('terminal_set_title', ...)` é pulado silenciosamente (sem erro, sem retry) — o rename nunca persiste. Antes desta correção 100% dos renames se perdiam (chave errada); agora só essa janela de corrida falha. Fica como follow-up para uma task futura, não para esta.
- **Anomalia investigada e descartada (11/08)**: o validador de T16 rodou `cargo test` (bônus, fora do gate desta task) e viu 1 falha em `projects::service::tests::cor_explicita_ja_usada_recusa` — coincidiu com a janela em que o validador de T5, em paralelo, tinha mutado temporariamente `service.rs` para um teste de mutação (revertido logo depois). Confirmado após os dois validadores terminarem: `git diff --stat -- src-tauri/src/projects/service.rs` → 242 inserções/1 deleção, batendo com o estado final restaurado. Falso alarme por concorrência entre validações, não defeito real.
- **`projects/T9`, achado do validador (não bloqueia)**: a idempotência de `sandbox_dir()` sob concorrência real (múltiplas threads/processos simultâneos) foi sustentada por leitura da stdlib (`DirBuilder::create_dir_all`), não por um teste que de fato dispara N threads e faz `join`. O teste real só exercita 2 chamadas sequenciais. Risco baixo (é o comportamento documentado da stdlib), mas fica registrado como o que a suíte automatizada NÃO prova sozinha.
- **`release-distribution/T3`, ressalva residual (já anotada em `tasks.md::T3`)**: o mapeamento `feat`/`fix`/`docs` → grupo legível segue provado só por leitura estática de `cliff.toml`, nunca por um commit `feat:`/`fix:` real passando pelo `git-cliff` — o histórico deste repo não tem nenhum commit assim, e criar um exigiria `git commit`, proibido para o agente aqui. Sem ação possível dentro das regras deste projeto.
- Itens já listados no `TRIAGE.md` (ACL das janelas secundárias, `REL-08`/`REL-22`, bytes do binário, threshold de cor) seguem não verificados — nada nesta run tocou essas frentes.
