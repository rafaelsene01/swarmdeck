# STATE — Memória do projeto

Última atualização: 03/08/2026 (sessão de observação ao vivo — picker de projeto/agente e restauração de sessão, `tlc-spec-driven`, após a spec-triage 006)

---

## Decisões

| Data | Decisão | Razão |
|---|---|---|
| 28/07/2026 | Stack: **Tauri 2 + Rust + React/TS** em vez de Electron | Binário ~10MB vs ~150MB, startup mais rápido, e o usuário já trabalha com Tauri em outro projeto. Custo aceito: PTY e integrações precisam ser reescritos em Rust. |
| 28/07/2026 | v1 cobre **apenas o núcleo gratuito** do original | É o único conjunto de features observável na instalação de referência. Especificar features PRO seria inventar comportamento. |
| 28/07/2026 | **Sem camada de monetização** no clone | O original usa tiers Starter/Pro para gate de features. O clone não tem produto comercial atrás — tudo construído é liberado. |
| 28/07/2026 | ~~Specs derivadas de **observação de UI**, não de código~~ — **revogado em 03/08/2026** (ver AD desta data: SwarmDeck deixa de ser descrito como clone) | Nenhum arquivo de produto de terceiros foi lido. Funcionalidade não é protegida por copyright; código e assets são. Manter essa separação limpa. — *decisão histórica, mantida como registro do que orientou as specs até aqui* |
| 28/07/2026 | Kanban em **janela separada**, como no original | Comportamento observado e defensável: o board é uma superfície de leitura/gestão distinta do trabalho no terminal. |
| 28/07/2026 | Servidor MCP como **sidecar separado**, não embutido no app | Não é escolha de estilo: o agente de CLI spawna servidores MCP como subprocesso stdio, e um app já rodando não pode ser esse subprocesso. |
| 28/07/2026 | Sidecar fala com o app por **IPC local**, e não escreve no banco direto | O app precisa possuir a escrita para empurrar `task_changed` às janelas. Escrita direta impossibilitaria o requisito de refletir no Kanban em < 1s. |
| 28/07/2026 | Saída do PTY via **`tauri::ipc::Channel`** com agregação de 16ms | Documentação do Tauri recomenda `Channel` para streaming e rate limiting explícito; `emit` por chunk derruba a UI sob saída volumosa. |
| 28/07/2026 | Regra "não pula a fase de teste" aplicada pela **ausência de aresta** na máquina de estados | Uma transição que não existe não pode ser esquecida; uma checagem `if` pode. |
| 28/07/2026 | **Sem arrastar cards** entre colunas no v1 | Não observado no original e conflita com a máquina de estados — arrastar para Completed pularia a fase de teste. |
| 28/07/2026 | Pipeline de CI/release **espelhado no `rafaelsene01/local-mind`**, adaptado | Pedido explícito do usuário. Os dois workflows de lá foram lidos na íntegra e servem de padrão validado em produção (o repositório já publicou releases reais). Adaptações obrigatórias: sai `protoc` (não há `lancedb`), sai `NO_STRIP` (não há binário de terceiro vendorizado), e o escritor de versão passa a editar o `Cargo.toml` **da raiz** — aqui `src-tauri` usa `version.workspace = true`. |
| 28/07/2026 | Release **só** por `workflow_dispatch`; push nunca publica | A garantia é estrutural: o `release.yml` não tem gatilho de `push`, `tag` nem `schedule`. Uma regra que depende de alguém lembrar não é uma regra. |
| 28/07/2026 | Escopo do updater: **pipeline completo** — assinatura, zip portátil e patch do `latest.json` | Escolha do usuário em 28/07/2026, ciente do custo: exige `tauri-plugin-updater` no app, geração do par de chaves e cadastro de secrets antes do primeiro release funcionar. O público-alvo roda em máquina corporativa onde instalador pede administrador; sem o portátil, esse público fica de fora. |
| 28/07/2026 | Versão do app **derivada** de `package.json` pelo `tauri.conf.json`, e `mainBinaryName` fixado em `SwarmDeck` | Uma cópia a menos para sincronizar, e um modo de falha barulhento (caminho inválido quebra o build). O binário hoje sai `swarmdeck.exe` enquanto o `productName` é `SwarmDeck` — divergência que apareceria dentro do zip portátil. |
| 28/07/2026 | **Um único módulo** (`paths.rs`) decide onde os dados moram | O modo portátil grava ao lado do executável e o instalado usa `app_data_dir()`. Com dois pontos de decisão, a diferença vaza. Como nada no código resolve caminho ainda, isso nasce certo em vez de ser reforma. |
| 28/07/2026 | **Contrato de ferramentas MCP congelado a partir do `CLAUDE.md` global**, sem validação prévia contra a implementação de referência | Escolha do usuário na triagem 001, ciente do trade-off apresentado: destrava `T0` e as 22 tarefas de M2 imediatamente, contra o risco de um nome divergir e quebrar em silêncio os prompts que ele já usa. O custo do erro é contido: o `rmcp` gera o schema a partir das assinaturas Rust, então renomear no código é barato — o caro é o prompt espalhado. Se um nome se provar errado depois, o conserto é rename + nota nesta tabela, não redesenho. |
| 28/07/2026 | **`Status: Draft` num `tasks.md` bloqueia execução automatizada** | Escolha do usuário na triagem 001. A `spec-loop` só executa feature cujo `tasks.md` esteja `In Progress` — hoje, só `multi-terminal`. Draft passa a significar "ainda não revisado pelo mantenedor", não "rascunho de forma". Consequência medida: a fila da run 001 cai de 24 para 7 tarefas. Para liberar uma feature, o mantenedor troca o `**Status**` dela e uma triagem nova reabre a fila. |
| 28/07/2026 | `cargo fmt --check` entra no CI agora; `clippy -D warnings` fica em P3 | `fmt` foi **medido** nesta sessão e passava (exit 0). Clippy não foi medido — ligar sem medir transformaria "introduzir CI" numa refatoração de escopo desconhecido. **⚠️ Remedido na triagem 001 (28/07/2026): `cargo fmt --check` agora sai com exit 1.** ~~7 arquivos com diff~~ — **corrigido na triagem 002: são 7 _hunks_ em apenas 3 arquivos** (`src-tauri/src/db/mod.rs`, `src-tauri/src/terminal/throttle.rs`, `src-tauri/tests/session.rs`). A triagem 001 contou linhas `Diff in` e as chamou de arquivos; o `rustfmt` emite uma por trecho, não por arquivo. Remedido por `cargo fmt --check | grep '^Diff in' | sed 's/:[0-9]*:$//' | sort -u | wc -l` → 3. A decisão continua válida; o que envelheceu foi o número. Formatar é pré-requisito de `release-distribution/T1`, senão o primeiro CI nasce vermelho. **⚠️ Remedido de novo na triagem 004 (01/08/2026): são outros 4 arquivos** — `src-tauri/src/commands/terminal.rs`, `src-tauri/src/terminal/manager.rs`, `src-tauri/tests/layout.rs`, `src-tauri/tests/manager.rs`. Nenhum dos 3 arquivos da correção anterior aparece mais na lista — o código formatado da triagem 002 permanece formatado; o desvio novo veio das tarefas escritas depois (T5, T10, T11, T13). Item continua aberto no inventário como `code`. |
| 28/07/2026 | **Tarefa cujo `Verify` exige o app rodando é `uat-agent`: o agente dirige o app** — e **nenhuma dupla `uat-agent` roda em paralelo**, mesmo marcada `[P]` | Escolha do usuário na triagem 002. Atinge `multi-terminal/T6, T7, T9, T10, T11`. O `[P]` da spec vale contra colisão de **arquivo**; ele não sabe que duas tarefas disputam a mesma **janela do app** — duas instâncias brigando pelo mesmo banco e pelo mesmo PTY dão falha que não se reproduz. Trade-off aceito: a fila executável sobe de 4 para 9 itens, contra o risco de a instabilidade de clique já registrada em § Lições produzir um "verificado" que ninguém viu. Contido por duas regras escritas em `multi-terminal/tasks.md`: **reler o screenshot** para confirmar que a ação aconteceu (nunca assumir que o clique pegou), e **verificação não confirmada não fecha a task** — "não consegui confirmar" é resultado válido, ✅ inventado não é. |
| 31/07/2026 | **`release-distribution` liberado para execução (`Draft` → `In Progress`); `agent-selection` permanece `Draft`** | Escolha do usuário na run 003. A AD de 28/07/2026 faz de `Draft` um bloqueio de execução automatizada; o usuário revisou e liberou **apenas** o `release-distribution`. Consequência medida: a fila desta run cobre `release-distribution` (Fases A/B + `T13`) e `multi-terminal/T5–T11`, e **exclui `agent-selection` inteira** — que era o segundo bloco pedido. Para liberá-la depois, basta trocar o `**Status**` do `tasks.md` dela. |
| 31/07/2026 | **Nenhum push nesta run: tarefa com gate `pipeline` para antes da execução no Actions** | Escolha do usuário na run 003. Atinge `rd/T2`, `T21`, `T6`, `T9–T12`, `T19`. O artefato (o YAML) é escrito e commitado localmente, mas a tarefa **não fecha** — `TESTING.md` é explícito: "não está pronta quando o YAML parseia; está pronta quando o run apareceu na aba Actions". Trade-off aceito: o repositório ganha o `ci.yml` sem que ninguém tenha provado que ele passa. Fechar exige um push humano. |
| 31/07/2026 | **O `Verify` de `uat-agent` não é executável neste ambiente e sai como NÃO CONFIRMADO** | O ambiente desta run não oferece screenshot nem clique na janela do app, então a AD de 28/07/2026 ("quem executa a tarefa também executa esse `Verify`, dirigindo o app") não tem como ser cumprida. Escolha do usuário: implementar, rodar o gate automatizado, commitar, e registrar o UAT como pendente humano — em vez de inventar um ✅. É a regra 3 de `TESTING.md` aplicada ao pé da letra. Atinge `multi-terminal/T6, T7, T9, T10, T11`. |
| 31/07/2026 | **`plugins.updater.endpoints` corrigido para `swarmdeck`; `pubkey` permanece a chave do `local-mind`** | O `tauri.conf.json` vinha do commit `94b9fcc` com `endpoints` **e** `pubkey` apontando para `rafaelsene01/local-mind` — o Verifier da run 003 mostrou que, quando `T15` ligasse o updater, o SwarmDeck buscaria o manifesto alheio e confiaria na assinatura de outro produto. O usuário aprovou zerar o `pubkey` nesta run, mas reverteu essa parte manualmente na árvore de trabalho logo em seguida, mantendo a chave do `local-mind` — só o `endpoint` ficou como `swarmdeck`. Estado real, não o aprovado originalmente: **`pubkey` continua sendo a chave errada**, agora sem o `endpoint` que a acompanhava. `T5` (`tauri signer generate`) precisa substituir esse campo antes de qualquer release — sem isso, uma assinatura válida do `local-mind` continuaria sendo aceita para atualizar o SwarmDeck. `plugins.updater.windows.installMode: "passive"` continua no arquivo e não está previsto no `design.md` — pendência de `T5`. |
| 31/07/2026 | **`Done when` de `release-distribution/T1` corrigido: o comando que ele prescrevia era impossível** | A spec exigia a string literal `"node --test scripts/"` no `package.json` **e** que `npm run test:scripts` passasse. No Node 24 o argumento posicional de `node --test` é glob, casa a própria pasta `scripts` e falha com `MODULE_NOT_FOUND` — não é quirk de Windows, reproduz em `ubuntu-latest`, que é o runner fixado no `ci.yml`. O Implementer alegou bug de plataforma; o Verifier desmentiu rodando `node --test` sem posicional (10 testes passam). Adotada a forma glob `node --test "scripts/**/*.test.mjs"`, restrita a `scripts/`, e o `Done when` foi corrigido junto com o código — corrigir só o `package.json` deixaria spec e código divergindo, que é o que a regra 4 de `spec-driven-changes.md` proíbe. |
| 31/07/2026 | **Commit atômico por tarefa não vale neste repositório; o mantenedor commita à mão** | `.claude/settings.json` tem `git commit` e `git push` no `deny`, então o delta de auto-commit da `spec-driven-execution` está estruturalmente desligado aqui. Consequência medida na run 003: os três commits que existem (`86bd32f`, `c3181df`, `e023808`) vieram de fora do pipeline, não usam as mensagens que o `tasks.md` prescreve, e o `c3181df` empacota cinco tarefas. Pior: **`HEAD` não passa em `cargo fmt --check`** — o conserto ficou nos arquivos não commitados. Escolha do usuário: recommitar à mão a partir do mapa arquivo→tarefa→mensagem, em vez de liberar `git commit` para o agente. |
| 31/07/2026 | **Exceção escrita na regra do marcador `SPEC:`: arquivo compartilhado leva marcador localizado** | O Verifier apontou `Cargo.toml:19` e `src-tauri/src/lib.rs:19` como desvio real do §3 de `.claude/rules/spec-driven-changes.md` ("no topo do arquivo"). O usuário aceitou o argumento — um marcador no topo de um manifesto de workspace mentiria sobre o resto do arquivo, e `lib.rs` ainda vai ser tocado por `multi-terminal/T6` — e mandou **escrever a exceção na regra**, com duas condições obrigatórias (bloco delimitável, e o `grep` continua achando). O que não se aceitou foi deixar a regra dizendo uma coisa e o código fazendo outra: aí o próximo executor faz diferente e ninguém sabe qual está certo. |
| 01/08/2026 | **Nenhum push nesta run, de novo: `release-distribution/T2` e `T21` seguem `human-only` apesar de implementadas e verificadas localmente** | Escolha do usuário na triagem 004, reconfirmando a AD equivalente da run "003" (31/07/2026) em vez de deixá-la presumida. Atinge `T2` (CI) e `T21` (Clippy no CI) — ambas com todo o `Done when` verificável localmente já `[x]`, faltando só "run verde na aba Actions". Consequência: `HEAD` continua 2 commits à frente do `origin/master`, e a cadeia `T6, T9–T12, T19` (que depende de T2 publicado) permanece fora de alcance nesta run. |
| 01/08/2026 | **`agent-selection`, `mcp-task-server` e `projects` liberados (`Draft` → `In Progress`); `terminal-statuses` e `task-kanban` permanecem `Draft`** | Escolha do usuário na triagem 004. Consequência medida: `code` retido por `Draft` cai de 27 para 10 tarefas (`terminal-statuses` 4 + `task-kanban` 6). `mcp-task-server/T0` (contrato de ferramentas MCP) passa a ser executável — é o gate de bloqueio de todo o M2. `agent-selection` (4 tarefas) não tem dependência pendente e fecha o M1 se completada. `projects` (4 tarefas) foi liberada junto, mas sua execução real só começa depois de `mcp-task-server/T1` (migração `002`) rodar — ficou classificada `In Progress` na spec para não exigir uma segunda triagem só para isso, mas a fila da `spec-loop` não a alcança nesta onda. |
| 01/08/2026 | **`mcp-task-server/T1` rodou como migração `003`, não `002` (texto do `tasks.md` ainda cita `002`)** | Consequência mecânica da run `spec-loop` 004: `release-distribution/T14` rodou antes na mesma run e reservou a `002` primeiro (regra "quem chegar primeiro pega o número", já escrita em `EXECUTION.md`). O código e os testes usam `003` corretamente; só o texto de `tasks.md` (`### T1: Migração \`002\`...`) ficou desatualizado. **Remediado na triagem 005 (02/08/2026):** os campos `Onde` e `Verify` de T1 foram corrigidos para apontar para `003_tasks.sql` e para os nomes reais dos testes (`migracao_003_e_idempotente` etc.); o título da seção continua dizendo `002` de propósito — é a nota histórica, já explicada no próprio arquivo, e reescrevê-lo apagaria o rastro da decisão. `EXECUTION.md:28` (label do diagrama) também corrigido. |
| 02/08/2026 | **A árvore foi publicada em `origin/master` (`cf21c82`) fora desta skill, e o CI real rodou duas vezes — as duas falharam** | Descoberto na triagem 005 ao abrir uma sessão nova: `git fetch` mostrou `origin/master` já em `cf21c82`, idêntico ao HEAD local — alguém (não uma run desta skill) fez `git push` entre o fechamento da triagem 004 e o início desta. As AD de 31/07 e 01/08 ("nenhum push autorizado") descreviam a intenção até então, mas a realidade divergiu; registrada aqui para não ficar presumida como ainda válida. Duas execuções do `.github/workflows/ci.yml` aconteceram (`gh run list`): run `30752379391` (push do commit `2169884`, 14:33 UTC) falhou no job `rust` por 2 testes de unidade de `catalog.rs` sensíveis a maiúsculas em nome de arquivo (bug real, Linux é case-sensitive); run `30754389304` (push do commit `cf21c82`, que já continha um fix para isso) chegou mais longe — `clippy` e `frontend` passaram, `cargo fmt --check` passou, mas `cargo test` falhou em `agent_prefs.rs` (`agente_padrao_removido_do_sistema_cai_para_o_primeiro_disponivel_e_avisa`, `sem_preferencia_gravada_usa_o_primeiro_agente_instalado`) — as duas esperavam `Some("codex-cli")` e receberam `None`, ou seja, a detecção de PATH isolado não achou nem o binário falso `codex` no ambiente Linux do runner, apesar do mesmo mecanismo passar localmente no Windows e nos testes unitários de `catalog.rs` da mesma run. Causa raiz não diagnosticada nesta triagem — investigar é trabalho de implementação, fora do escopo desta skill. `T2`/`T21` (`release-distribution`) continuam sem fechar, mas agora por um motivo diferente do que a spec registrava: não é mais "falta rodar", é "rodou duas vezes e as duas quebraram" — ver item no inventário. **Atualização, ainda 02/08/2026, mesma sessão:** enquanto esta triagem estava em andamento, o mantenedor corrigiu a causa raiz **fora desta skill**, em paralelo — commit `5ac0ecd` (`git log` mostra HEAD avançou de `cf21c82` para `5ac0ecd` durante a sessão): `PathIsoladoGuard::with_only_codex_instalado` escrevia sempre `codex.exe`, mesmo fora do Windows, tornando o binário falso invisível para `catalog::command_exists_in_dir` no Linux (que procura `codex` sem extensão); a correção usa `cfg!(windows)` para escolher o nome certo, mesmo padrão que `cf21c82` já tinha aplicado em `catalog.rs`. A lógica confere por leitura; **não foi provada em CI real** (seria preciso outro push, e a decisão desta triagem foi não mexer nisso agora). Nota também: `scripts/make-portable.mjs`, `scripts/patch-latest-json.mjs` e `.github/workflows/release.yml` apareceram como arquivos novos não rastreados durante esta mesma sessão — implementação em andamento de `release-distribution/T7, T8` fora desta skill, não auditada nem incorporada ao inventário desta triagem. |
| 03/08/2026 | **O release realmente saiu: `origin/master` em `5c81ed2` (tag `v0.1.1`), publicado fora desta skill entre a triagem 005 e a 006** | Descoberto pelo Auditor A da triagem 006 (`git fetch` + `gh run list`): `Release` rodou via `workflow_dispatch` com sucesso (`30778409874`, 03/08/2026 02:05 UTC) e `CI` passou limpo no commit `259b96e` (`30778057309`, 01:56 UTC) — depois de duas falhas anteriores já documentadas (AD de 02/08/2026, bug de `agent_prefs.rs` no Linux). Isso resolve, na prática, o bloqueio que `release-distribution/T2` e `T21` registravam. **Não re-auditado tarefa a tarefa**: `T5` (chave de assinatura), a cadeia `T6-T12` e `T19` (verificação ponta a ponta) continuam com o texto de `tasks.md` não reconciliado contra este release real — próxima triagem deveria abrir uma passada dedicada só nisso, comparando cada `Done when` contra o artefato publicado de verdade (o `.msi`/`-setup.exe`/AppImage/zip da release `v0.1.1`). `T3` (`git-cliff`) segue **não resolvida**: `git cliff --version` continua falhando neste ambiente. |
| 03/08/2026 | **Observação ao vivo de 4 telas novas do produto de referência: fluxo de criação de terminal (picker de projeto → agente) e restauração após crash** — nascem `PROJ-06..09`, `TERM-12`, `AGT-06` | Usuário anexou 4 capturas de tela reais em `.specs/research/screenshots/`: `Captura de tela 2026-08-03 003525.png` (passo PROJECT, lista de projetos com nome/caminho/"5 days"), `003640.png` (modal "Create New Project" — paleta de cor clicável, ícone opcional, checkbox de git), `003653.png` (passo AGENT — Resume/New Session, pseudo-projeto "Sandbox", toggles Turbo/Git Worktree), `004512.png` (modal "Restore Previous Session" no boot). Cinco decisões tomadas em conjunto com o usuário, todas registradas nas specs correspondentes: (1) **cor do projeto vira sugestão automática com override manual** — revoga a suposição inicial desta mesma sessão ("automática, só exibida"); a captura mostrou a paleta claramente clicável, e o usuário confirmou que é assim mesmo, revisando `PROJ-01`/`PROJ-02`; (2) **Turbo Mode confirmado observado na UI** (toggle ao lado de "Git Worktree"), mas o usuário manteve a exclusão já registrada em `agent-selection/spec.md` — a linha da tabela de "Fora de escopo" não é revogada, só deixa de dizer "não observável" para dizer "observado e propositalmente excluído"; (3) **"Resume Session" entra no núcleo v1** (`AGT-06`) — mesmo se aproximando da feature PRO "Conversation History" do original, o usuário decidiu construir porque o clone não tem paywall; é mais restrito que o original (só a última sessão do par projeto+agente, não histórico buscável); (4) **"Initialize as Git repository" é local-only** (`PROJ-09`) — só `git init`, sem remoto, sem GitHub; quando não há remote configurado, alguma UI (header do terminal ou lista de projetos) precisa sinalizar isso, decisão do usuário ao ser perguntado sobre a implicação do checkbox; (5) **conectar conta GitHub fica fora desta rodada** — o usuário chegou a sugerir uma seção "Conectar GitHub" no `settings-shell` (entre Notificações e Agente padrão), mas ao ser perguntado se isso era pré-requisito do checkbox de git, escolheu "só git init local" — a ideia de GitHub/OAuth vai para "Ideias adiadas" abaixo, não vira feature nesta sessão. **Também corrigido nesta sessão**: o componente hoje chamado `NewTerminalDialog` não é um modal flutuante — a observação mostrou que o passo PROJECT→AGENT renderiza **dentro do próprio slot de terminal vazio**; o único modal de verdade observado é "Create New Project". Isso não muda nenhum critério de aceite de `TERM-10`/`TERM-11` (ainda não implementados — a correção chegou a tempo, antes do código), só a moldura em que eles vivem; `multi-terminal/design.md` e `tasks.md` (T15, T17) foram atualizados para refletir isso. Specs tocadas: `projects/spec.md` (+`PROJ-06..09`, revisão de `PROJ-01`/`PROJ-02`), `multi-terminal/spec.md` (+`TERM-12`, correção de contexto em `TERM-10`), `multi-terminal/design.md` (componentes `RestoreSessionModal`, `terminal::shutdown`, correção de `NewTerminalDialog`), `agent-selection/spec.md` (+`AGT-06`, nota sobre Turbo, cross-ref com `worktrees/WT-01`). `tasks.md` das três features ganharam tarefas novas (`projects/T5-T9`, `multi-terminal/T17-T20`, `agent-selection/T6`), todas `Pending`, nenhuma implementada ainda. Alguns detalhes ficaram **não confirmados** (não inventados): comportamento exato de "Import Project" após escolher a pasta, e o significado do contador "N/M projects" — registrados como pendência em `projects/spec.md`, não como fato. |
| 03/08/2026 | **Settings inteiro é inatingível num app real: `AgentPanel`, `ProjectsPanel`, `StatusesPanel`, `UpdateSettings` existem, testados isoladamente, mas nenhum é importado fora do próprio arquivo de teste — não existe janela nem rota de Settings em lugar nenhum** | Achado do Auditor A (agent-selection, projects) e do Auditor B (terminal-statuses) da triagem 006, de forma independente e convergente, e reconfirmado pelo verificador da triagem 007 (03/08/2026): `grep -rln "AgentPanel\|ProjectsPanel" src` só acha os próprios arquivos + `NewTerminalDialog.tsx`, que importa só o **tipo** `AgentDescriptor` (`import type {...} from '../../routes/settings/AgentPanel'`) — não monta nem renderiza o componente. `src/main.tsx`/`App.tsx` não têm router nem alternância por janela para um caminho de Settings (o único precedente de janela secundária é `windows/kanban.rs`, que a própria `task-kanban/design.md` documenta como pendente de rota real). Afeta `agent-selection` (AGT-01..04), `projects` (PROJ-01, PROJ-02, PROJ-05 — a resolução automática PROJ-03/04 roda no backend e não depende da UI), `terminal-statuses` (STAT-02/03, o CRUD de catálogo) e `release-distribution` (toggle de auto-check do `UpdateSettings`). **Vira pergunta para o usuário na Fase 3 desta triagem** — não decidido aqui: onde a janela/rota de Settings deveria morar (feature nova `settings-shell`, ou dentro de `agent-selection` por ter sido a primeira a precisar dela). |
| 03/08/2026 | **Mesmo onde a escolha de agente aparece na UI (`NewTerminalDialog`), ela é descartada: `App.tsx::handleCreate` recebe `agentId` e nunca o usa** | Achado do Auditor A: `src/App.tsx:77` tem `const handleCreate = (cwd: string, _agentId: string | null) => {` — o parâmetro é prefixado com `_` (convenção de "não usado") e nunca chega a `pty_spawn`. Mesmo se a lacuna de Settings acima for resolvida, a sobrescrita por sessão (AGT-03) continuaria quebrada. Vira task nova em `agent-selection/tasks.md` (`code`, sem decisão pendente — é só passar o parâmetro adiante). |
| 03/08/2026 | **`AGENTS.md` está vazio (0 bytes) desde que foi criado — `CLAUDE.md` importa via `@AGENTS.md`, e `.claude/rules/spec-driven-changes.md` cita "em inglês, como todo comentário de código desta base (AGENTS.md)" como se o arquivo definisse a convenção** | Achado do orquestrador da triagem 006 direto (`git log --follow -- AGENTS.md` → criado vazio no commit `94b9fcc`, 30/07/2026, nunca editado desde então). A convenção (comentários de código em inglês) é seguida na prática — todos os marcadores `// SPEC:` conferidos estão em inglês — mas a fonte que supostamente a declara está vazia. Vira pergunta para o usuário na Fase 3: popular `AGENTS.md` com as convenções reais (idioma, marcador `SPEC:`, política de commit) ou deixar como está por ser um gap sem consequência prática medida até agora. |
| 03/08/2026 | **Nasce a feature `settings-shell`: janela dedicada para os 4 painéis de configuração órfãos** | Decisão do usuário na triagem 006, respondendo à pergunta sobre onde `AgentPanel`/`ProjectsPanel`/`StatusesPanel`/`UpdateSettings` deveriam morar (ver AD acima, "Settings inteiro é inatingível"). Opção escolhida: feature nova, não dentro de `agent-selection`. Arquitetura decidida junto (não é gray-area aberta): segunda janela Tauri, mesmo padrão de `windows/kanban.rs` — não modal, sem `react-router`, branch por `label` de janela em `main.tsx` (estende a mesma decisão de `task-kanban/T7`). `.specs/features/settings-shell/{spec,tasks}.md` criados, `SET-01`/`SET-02`, `T1`/`T2`. `agent-selection/tasks.md` (nota `⛔ NEEDS-DECISION` resolvida), `projects/ROADMAP.md`, `terminal-statuses/ROADMAP.md`, `release-distribution/ROADMAP.md` atualizados para apontar para ela. |
| 03/08/2026 | **`TERM-06` (multi-terminal) absorve o requisito de `STAT-07` (terminal-statuses), revogada** | Decisão do usuário na triagem 006: as duas specs descreviam a mesma regra ("rename manual do terminal vence o agente"). `TERM-06` fica dono único — já era mais amplo (também cobre branch/git no header) e mora na feature que já tem `TerminalHeader`. `STAT-07` riscada em `terminal-statuses/spec.md` (traceability + AC5 da história "Título geral vs atividade"), com o motivo e a data. Task nova `multi-terminal/tasks.md::T16` implementa a parte de rename (a lógica de domínio já existe e já é testada em `terminal::meta::TerminalMetaService::set_title`, feature `mcp-task-server` — falta só o comando Tauri e a UI). A parte de branch/git de `TERM-06` continua sem task, não agendada nesta triagem. |
| 03/08/2026 | **`AGENTS.md` populado com as convenções reais do projeto** | Decisão do usuário na triagem 006: popular em vez de deixar vazio. Conteúdo: idioma (código/commits em inglês, `.specs/**` em português), formato do marcador `SPEC:` (com link para a regra completa), política de commit (agente nunca commita/`push`, bloqueio estrutural em `.claude/settings.json`), tabela de comandos de gate, e o princípio de que `.specs/` descreve o projeto de hoje, não um histórico. Fecha o gap em que `CLAUDE.md` (`@AGENTS.md`) e `.claude/rules/spec-driven-changes.md` citavam o arquivo como fonte de convenção enquanto ele tinha 0 bytes desde a criação (commit `94b9fcc`, 30/07/2026). |
| 03/08/2026 | **`EXECUTION.md` diz "38 tarefas" — hoje são 43** entre `multi-terminal`(15) + `agent-selection`(4) + `projects`(4) + `mcp-task-server`(10) + `terminal-statuses`(4, antes da T5 desta triagem) + `task-kanban`(6, antes de T7/T8 desta triagem) | Achado do Auditor A (`grep -cE '^### T'` nos 6 `tasks.md`). Ficou desatualizado depois que `multi-terminal` ganhou `T13-T15` e `mcp-task-server` ganhou `T9`, sem recontagem em `EXECUTION.md`. Com as tasks novas desta triagem (`task-kanban/T7,T8`, `terminal-statuses/T5`), o número real passa a ser 46. **Não corrigido em `EXECUTION.md` nesta triagem** — arquivo não tocado, fica para a próxima passada (é só recontagem, sem decisão). |
| 02/08/2026 | **Diretório do diálogo de novo terminal: campo vira somente-leitura, preenchido só pelo seletor nativo de pasta do SO (não `<input type="file">` de HTML)** | Pedido do usuário: "quero que quando aperto para abrir um novo terminal ele abra uma busca por pasta [...] devo selecionar a pasta em que o terminal vai usar como path para ser iniciado". Três perguntas feitas ao usuário e respondidas nesta sessão: (1) campo só-leitura em vez de editável + botão — elimina caminho digitado errado; (2) cancelar o seletor **limpa** o campo, não mantém o valor anterior; (3) o seletor abre no **último diretório usado** (persistido, não só home nem cwd do terminal focado) — exige nova tabela (`terminal_picker_prefs`, migração `005`) e dois comandos Tauri novos. Novos requisitos `TERM-10`/`TERM-11` em `multi-terminal/spec.md`, tarefas `T13, T14, T15` em `multi-terminal/tasks.md` (Fase 5), design apendado em `multi-terminal/design.md`. **Nada implementado ainda** — só spec/design/tasks. Nota separada, fora do escopo desta demanda: `ROADMAP.md:32` registra `T12` como pendente e `App.tsx` como "ainda o placeholder", mas a leitura de `App.tsx` nesta sessão mostrou a árvore real já montada (`GridLayout`/`TerminalPane`/`TerminalHeader`/`NewTerminalDialog` importados e renderizados) — indício de que `T12` foi implementado fora desta skill, sem o `tasks.md`/`ROADMAP.md` terem sido atualizados; não corrigido aqui por estar fora do que foi pedido, mas fica registrado para a próxima triagem confirmar. |
| 01/08/2026 | **⛔ NEEDS-DECISION aberta: falta task de integração para `App.tsx` — `multi-terminal/T7, T9, T10, T11` não têm `Verify` confirmável** | Achado do `uat-agent` na run `spec-loop` 004: pela primeira vez o ambiente tinha display real e o agente conseguiu subir o app e dirigir por CDP — mas `src/App.tsx` continua sendo o placeholder do scaffolding, sem importar `GridLayout`/`TerminalPane`/`TerminalHeader`/`state/terminals.ts`. `T6` foi confirmado (só precisa do console do DevTools, sem UI). Os outros quatro passam nos gates isolados mas são inatingíveis por um usuário real. Nenhuma task lista `App.tsx` como "Onde" — a integração nunca foi planejada como item explícito. Estacionado em `multi-terminal/tasks.md` (T7, T9, T10, T11) e no `JOURNAL.md` da run 004. **✅ Resolvida na triagem 005 (02/08/2026):** o usuário escolheu criar uma task nova (não reabrir uma existente) — é `multi-terminal/T12` ("Montar `App.tsx`"), pronta para a `spec-loop`. `T7, T9, T10, T11` ficam com `Verify` pendente dela. |
| 02/08/2026 | **⛔ NEEDS-DECISION: `IpcServer` (mcp-task-server/T5) nunca é iniciado pelo app real — sidecar não conecta em produção** | Achado do auditor na triagem 005: `grep -rn "IpcServer" src-tauri/src/lib.rs` → nenhum resultado. Todos os 16 testes de T5/T6/T7 passam porque rodam contra um `IpcServer` instanciado dentro do próprio teste, nunca contra o app de verdade — mesma classe de gap que o de `App.tsx` (gate isolado verde, integração real nunca planejada como task). Nenhuma task lista `src-tauri/src/lib.rs`/`run()` como `Onde`; o próprio doc-comment de `IpcServer::for_app` (`ipc/server.rs:210-214`) já antecipava essa lacuna. **✅ Resolvida na triagem 005 (02/08/2026):** o usuário escolheu criar uma task nova — é `mcp-task-server/T9` ("Iniciar o `IpcServer` no app real"), pronta para a `spec-loop`. `task-kanban` (liberada na mesma triagem) depende de T5 segundo `EXECUTION.md`, mas para sincronizar de verdade também depende de T9. |
| 02/08/2026 | **CI Linux real ficou vermelho; decisão: não investigar nesta rodada** | Escolha do usuário na triagem 005, em resposta à AD acima (push já aconteceu, CI rodou 2x, as 2 falharam). Opção escolhida: "Não mexer nisso agora" — nem push adicional para iterar, nem correção local sem prova. `release-distribution/T2, T21` seguem exatamente como estavam (vermelhas, causa registrada), e o bug de `agent_prefs.rs` no Linux fica só documentado no inventário desta triagem, sem entrar na fila da `spec-loop`. |
| 02/08/2026 | **`terminal-statuses` e `task-kanban` liberadas (`Draft` → `In Progress`)** | Escolha do usuário na triagem 005: liberar as duas de uma vez, já que `mcp-task-server` (gate de bloqueio do M2 inteiro) fechou na run 004. Consequência: `code` retido por `Draft` cai a zero — as duas features (10 tarefas) entram na fila da próxima `spec-loop`, respeitando a dependência declarada em `EXECUTION.md` (`mcp-task-server/T4` → `terminal-statuses`; `mcp-task-server/T5` → `task-kanban`; a segunda também depende da `⛔ NEEDS-DECISION` de T5 acima ser resolvida primeiro, já que o Kanban precisa do `IpcServer` rodando de verdade para sincronizar em tempo real). |
| 03/08/2026 | **SwarmDeck deixa de ser descrito como clone/engenharia reversa de um produto nomeado; passa a ser tratado como projeto original** — revoga a AD de 28/07/2026 ("Specs derivadas de observação de UI") | Decisão do usuário nesta sessão. `PROJECT.md`, `README.md`, `UI-INVENTORY.md` e as notas de proveniência em `agent-selection/spec.md`, `multi-terminal/spec.md` e `projects/spec.md` deixam de nomear o produto de referência ("CodeAgentSwarm de referência" → "produto de referência" ou equivalente genérico). O histórico de *por que* cada requisito existe é preservado — datas, screenshots e critérios de aceite não mudam, só o nome próprio sai do texto. **Exceção mantida por decisão explícita do usuário**: o contrato de ferramentas MCP (`mcp-task-server/TOOL-CONTRACT.md`) continua compatível de propósito com a seção de gerenciamento de tarefas do `CLAUDE.md` global do usuário — só a citação nominal do produto sai da prosa; os nomes de ferramenta e a variável `CODEAGENTSWARM_CURRENT_QUADRANT` (`mcp-task-server/design.md`) não mudam, por ser requisito funcional de compatibilidade, não narrativa. **Não estendido nesta rodada**: a linguagem genérica "do original" / "instalação de referência" (sem citar o nome) segue presente em ~15 outros arquivos (`mcp-management/spec.md`, `conversation-cleanup/spec.md`, `task-kanban/spec.md`, `release-distribution/design.md` etc.) — fora do pedido original, que era especificamente sobre o nome "CodeAgentSwarm". |
| 02/08/2026 | **2 bugs reais achados em `release-distribution/T6, T7` — a documentação dizia "só falta o disparo real", mas o código quebraria mesmo com o disparo** | Pedido do usuário: auditar a parte de `release-distribution` (T6-T11) que uma sessão concorrente implementou durante esta mesma triagem (ver entrada acima sobre `5ac0ecd`) e que ainda não tinha sido auditada. Um segundo subagent somente-leitura leu `release.yml`, `make-portable.mjs` e `patch-latest-json.mjs` linha a linha. Achado 1 (ALTA, bloqueia toda a Fase C): o step "Commitar e taguear" de `T6` (`release.yml:135`) faz `git add ... src-tauri/Cargo.toml src-tauri/Cargo.lock`, mas nenhum dos dois existe — a versão foi movida para o `Cargo.toml` da **raiz** (decisão já registrada nesta spec), e `T1` (`bump-version.mjs`) já escreve lá corretamente; só o `git add` de `T6` ficou com o caminho do `local-mind` (repo de referência), nunca adaptado. Sob `set -euo pipefail`, isso derruba o job `prepare` antes de qualquer commit/tag/push — e como `T9`, `T10`, `T11` dependem de `prepare` (`needs:`), a cadeia inteira trava por este bug, não só por faltar `T5`/`T12`. Achado 2 (ALTA): `make-portable.mjs` resolve o binário default para `src-tauri/target/release/...`, mas o workspace Cargo builda em `<raiz>/target/release/` — `release.yml:225` chama o script sem `--binary`, então bateria nesse default quebrado. Os 6 testes do script passam porque todos passam `binary` explícito, nunca exercitando o default real. Mais ~6 divergências de MÉDIA/BAIXA gravidade: citações de linha erradas em `Done when` de T6, T7, T8, T9, T20 (números deslocados, corrigidos nos respectivos `tasks.md`/`spec.md`), e a afirmação de que `gh release edit --draft=false` roda num "job separado" em T10 — é o mesmo job `finalize`, dois steps sequenciais. Nenhuma correção de código foi aplicada (fora do escopo desta skill) — os dois bugs ALTA viram itens `code` no inventário da triagem 005, prontos para a `spec-loop` corrigir antes de qualquer tentativa de `T12`. |

---

## Bloqueios

| Item | Impacto | Estado |
|---|---|---|
| Features PRO atrás de paywall (Git, History, Permissions, Shortcuts, Labels, Turbo) | UI real não observável — não dá para especificar fielmente | **Aceito.** Fora do v1. Só a matriz de features e as descrições curtas do paywall foram registradas em UI-INVENTORY.md. |
| Protocolo MCP do original é um contrato com agentes externos | Se as ferramentas do clone não baterem com os nomes esperados, prompts existentes quebram | **Resolvido em 28/07/2026 (triagem 001), por decisão do usuário.** Os nomes inferidos do `CLAUDE.md` global viram o contrato — sem validação prévia contra a implementação de referência. Ver a AD abaixo e `mcp-task-server/T0`. |

---

## Lições

- A automação de clique na janela do app é **instável**: alguns cliques registram só como hover e a seção não troca. Sempre reler o screenshot para confirmar que a navegação aconteceu, em vez de assumir. Retry com espera maior resolve.
- O **paywall é a melhor fonte de inventário de features** de um produto freemium — expõe a lista completa de capacidades de uma vez, incluindo as que não dá para ver na UI.
- **Ler o código do crate desmentiu a pesquisa.** A busca dizia que precisávamos setar três flags de ConPTY; o fonte do `portable-pty` 0.9.0 mostra que dois já vêm hardcoded e o terceiro não é configurável. Pesquisa serve para saber onde olhar — o fonte é que decide.
- **Um `wait` sem prazo transforma bug em silêncio.** A suíte ficou 20 minutos travada sem emitir uma linha. Com prazo, o mesmo defeito virou uma falha em 25s **com a mensagem que continha a causa** (`\u{1b}[6n`). Em código que espera por processo externo, prazo não é robustez — é observabilidade.
- **Regra documentada não é regra aplicada.** `TESTING.md` já dizia que teste de PTY não é paralelizável, mas o harness do Rust paraleliza por padrão. Só passou a valer quando virou um guard de mutex dentro do próprio arquivo de teste.

---

## Todos

- [x] ~~Confirmar os nomes exatos das ferramentas MCP contra a implementação real, antes de codificar o servidor~~ — **revogado por decisão do usuário na triagem 001 (28/07/2026)**: o contrato congela os nomes inferidos, sem validação prévia. `T0` deixa de ser "confirmar contra a referência" e passa a ser "escrever o `TOOL-CONTRACT.md` a partir do `CLAUDE.md` global". O risco aceito está na AD.
- [ ] Capturar as superfícies pendentes listadas no fim de `UI-INVENTORY.md`
- [x] ~~Decidir formato de persistência do layout do grid (JSON em disco vs tabela SQLite)~~ — **sem objeto desde `multi-terminal/T2`**: a migração `001_terminal_layout.sql` criou a tabela `terminal_layout` e o código já a consome. Ficou SQLite. Riscado na triagem 001 (28/07/2026); a decisão foi tomada no código, não aqui.
- [x] ~~Verificar se `portable-pty` cobre resize e sinais no Windows~~ — confirmado: `MasterPty::resize(PtySize)` cobre, e ConPTY é suportado. **Novo item:** confirmar em qual versão do crate o flag `PSEUDOCONSOLE_PASSTHROUGH_MODE` é exposto (exige Win11 22H2+, precisa de fallback)
- [x] ~~Definir o algoritmo de similaridade de tarefas~~ — decidido e implementado em `mcp-task-server/T8` (run `spec-loop` 004, 01/08/2026): coeficiente de **overlap** (Szymkiewicz-Simpson) sobre trigramas de caractere, não Dice — a primeira tentativa usou Dice e falhou o próprio exemplo da spec ("Add pagination" vs "Implement pagination in the list" pontuava 0.43, exigido >0.70); a segunda passada de verificação encontrou o bug e trocou a métrica (agora 0.75).
- [x] ~~`D:\ide` **não é um repositório git** — inicializar antes do primeiro commit~~ — feito: branch `master`, remote `git@github.com:rafaelsene01/swarmdeck.git`, **3 commits** (remedido na triagem 001; eram 1 quando isto foi escrito), **zero tags**
- [ ] 🔑 **Bloqueante do release**: rodar `tauri signer generate` e cadastrar `TAURI_SIGNING_PRIVATE_KEY` / `TAURI_SIGNING_PRIVATE_KEY_PASSWORD` como secrets do repositório (`release-distribution/T5`) — passo humano, nenhuma tarefa automatizada substitui
- [x] ~~Reconfirmar as versões correntes de `tauri-plugin-updater` e `minisign-verify` antes de fixá-las~~ — reconfirmado em `release-distribution/T15`/`T16` (run 004, 01/08/2026): `tauri-plugin-updater 2.10.1`, `minisign-verify 0.2.5`, `semver 1.0.28`.
- [x] ~~Desempatar o número da migração de `settings`~~ — resolvido na run 004 (01/08/2026): `release-distribution/T14` rodou primeiro nesta run e pegou a `002`; `mcp-task-server/T1` rodou em seguida e pegou a `003` (o `tasks.md` de `mcp-task-server` ainda cita `002` no título de T1 por engano de texto — o código usa `003` corretamente, nota de correção já registrada lá).
- [ ] **`crates/swarmdeck-mcp` agora existe** (`mcp-task-server/T6`, run 004, 01/08/2026) — falta declará-lo como `externalBin` no bundle do Tauri, levá-lo dentro do zip portátil, e reavaliar se o AppImage passa a precisar de `NO_STRIP`. Nenhuma task de `release-distribution` cobre isso ainda — é um item novo, não coberto pela spec atual.
- [x] ~~Medir o tamanho do binário com e sem `strip`/LTO e comparar à meta de **< 20MB** do `PROJECT.md`~~ — medido em `release-distribution/T20` (31/07/2026), `swarmdeck.exe` release, `CARGO_TARGET_DIR` separado para cada build: **sem** o perfil enxuto, 9.849.344 bytes (≈9,39 MiB); **com** `strip = true` + `lto = "thin"` + `codegen-units = 1`, 7.805.440 bytes (≈7,44 MiB). Redução de 2.043.904 bytes (≈1,95 MiB, ~20,8%). **Meta de < 20MB atingida nos dois casos** — já estava dentro da meta mesmo sem o perfil; o perfil reduz mais ainda.
- [x] ~~Remedir o tamanho do binário (a medição de 31/07/2026 acima ficou desatualizada — muito código novo entrou desde então)~~ — remedido na run `spec-loop` 007 (03/08/2026, HEAD `259b96e`), `cargo build --release -p swarmdeck`, `CARGO_TARGET_DIR` separado para cada build (o build "sem perfil" rodou num `git worktree` descartável, removido ao final — a árvore real nunca teve o `Cargo.toml` editado): **com** o perfil atual, 13.596.672 bytes (≈12,97 MiB); **sem** `strip`/`lto`/`codegen-units`, 13.970.944 bytes (≈13,32 MiB). Redução de 374.272 bytes (≈0,36 MiB, **~2,68%** — bem menor que os ~20,8% de 31/07, porque o binário cresceu bastante desde então: `mcp-task-server`, `task-kanban`, `terminal-statuses`, `agent-selection`, `projects`, mecanismo de update). **Meta de < 20MB continua atingida nos dois casos**, com folga de ~6,7MB mesmo sem o perfil.
- [x] ~~Medir quantos warnings o `cargo clippy -D warnings` acusa hoje, antes de ligá-lo no CI~~ — medido em `release-distribution/T21` (31/07/2026): `cargo clippy --all-targets -- -D warnings` sai com **exit 0, zero warnings**, sem limpeza nenhuma necessária antes de ligar o job no CI.

---

## Ideias adiadas

- Modo **Tabs** de layout (o original tem Grid + Tabs; v1 entrega só Grid)
- Subtarefas e hierarquia pai-filho no Kanban — existe nas ferramentas MCP do original, mas nenhum card com hierarquia foi observado no board
- Atalhos de teclado por projeto e por terminal
- Labels/etiquetas em tarefas
- Marketplace próprio (MCP e Skills) — v1 lista e gerencia o que está instalado localmente
- **Code signing** (Authenticode / notarização) — depende de comprar certificado; sem ele o SmartScreen avisa na 1ª execução, aceitável no v1
- **macOS no pipeline** — o `PROJECT.md` já registra macOS como alvo não validado no v1, e o pedido foi Linux + Windows
- **Canal beta / pré-releases** — modelo de branch é `master` puro
- **Delta updates** — complexidade desproporcional ao tamanho do projeto
- **Zip portátil de Linux** — o `.AppImage` já roda sem instalar e já é suportado pelo updater oficial; um binário nu dependeria do `webkit2gtk` do sistema
- **Conectar conta GitHub (OAuth) e criar/vincular repositório remoto ao criar um projeto** — decisão do usuário em 03/08/2026 ao especificar `PROJ-09` ("Initialize as Git repository"): o checkbox fica local-only (`git init` puro) nesta rodada. Conectar GitHub abriria uma feature própria — fluxo OAuth, credenciais, e uma seção nova em `settings-shell` (o usuário sugeriu "entre Notificações e Agente padrão", mas isso nem chegou a ser desenhado)

---

## Preferências

- Usuário escreve em **português**; specs e documentação seguem o mesmo idioma. Identificadores de requisito e nomes de arquivo em inglês.
