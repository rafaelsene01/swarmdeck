{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability padrão da janela principal do SwarmDeck.",
  "windows": ["main"],
  "permissions": ["core:default", "updater:default"]
}
